apiVersion: apps/v1
kind: Deployment
metadata:
  name: hungryman-api-gateway
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: hungryman-api-gateway
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Staging"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
